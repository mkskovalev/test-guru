<nav class="navbar navbar-light bg-light">
  <div class="container-fluid">
    <%= link_to 'Test Guru', root_path, class: 'navbar-brand' %>
    <% if current_user.is_a?(Admin) %>
      <%= link_to 'Admin Panel', admin_tests_path %>
    <% end%>
    <span class="navbar-text">
      <% if user_signed_in? %>
          Welcome, <b><%= current_user.email %></b> Guru
          <%= button_to "Log Out", destroy_user_session_path, method: :delete, class: 'btn btn-outline-primary' %>
      <% else%>
        <div class="btn-group" role="group">
          <%= button_to "Sign Up", new_registration_path(resource_name), method: :get, class: 'btn btn-outline-primary' %>
          <%= button_to "Log In", new_session_path(resource_name), method: :get, class: 'btn btn-outline-primary' %>
        </div>
      <% end%>
    </span>
  </div>
</nav>

