<tr>
  <td><%= test.title %></td>
  <td><%= test.level %></td>
  <td><%= test.questions.count %></td>
  <% if test.questions.count > 0 %>
    <td><%= button_to t('.start_button'), start_test_path(test), class: 'btn btn-outline-primary' %></td>
  <% end %>
</tr>

